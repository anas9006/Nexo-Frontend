# Copy to .env.development or .env.production (or .env.local for overrides).
# Backend base URL only — no /api suffix, no trailing slash.

# Local development (npm run dev)
# VITE_API_URL=http://localhost:5000

# Production build (npm run build)
# VITE_API_URL=https://nexo-backend-moof.onrender.com
